# Deploys the static site per-branch (no build step):
#   main    -> GitHub Pages      (production, https://kennethho.ca)
#   staging -> Cloudflare Pages  (staging,    https://stg.kennethho.ca)
name: deploy site

on:
  push:
    branches: [main, staging]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-${{ github.ref }}"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  # Production -> GitHub Pages
  deploy-production:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7

      # The custom-domain marker (src/CNAME) is committed and ships in ./src.
      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload site
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./src

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

  # Staging -> Cloudflare Pages (Direct Upload via Wrangler)
  deploy-staging:
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # The SECRET banner reveals itself client-side from the stg.* hostname
      # (see src/index.html) — no build-time stamping needed here.
      # --branch=staging matches the Cloudflare project's PRODUCTION branch so
      # this deploy is served on the stg.kennethho.ca custom domain.
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./src --project-name=kennethho-stg --branch=staging
